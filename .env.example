PORT=4000
NODE_ENV=development
CORS_ORIGIN=http://localhost:5173
CACHE_TTL_SECONDS=300
MAX_GITHUB_PAGES=50

# Optional: enables GitHub GraphQL contribution calendar and raises API limits.
# Create a fine-grained or classic token with public read access only.
GITHUB_TOKEN=

# Frontend
VITE_API_URL=http://localhost:4000

